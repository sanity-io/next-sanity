{
  "name": "@fixtures/fail/server-only-live",
  "version": "0.1.0",
  "private": true,
  "description": "defineLive can not be called from a client component",
  "scripts": {
    "test": "next build && exit 1 || exit 0",
    "type-check": "next typegen && tsc --noEmit"
  },
  "dependencies": {
    "next": "catalog:",
    "next-sanity": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:"
  }
}
