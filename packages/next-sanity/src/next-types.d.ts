// `next/types/global.d.ts` types `process.env.NODE_ENV` and the `next` option
// on `fetch`. Until v14 it reached the program through the studio entry's
// `import type {Metadata, Viewport} from 'next'`; this keeps it explicit.
/// <reference types="next" />
