---
'next-sanity': minor
---

Add `onReconnect` prop on `<SanityLive>`, by default it calls logs an error to the console. You can set `<SanityLive onReconnect={false} />` to disable the default behavior.
